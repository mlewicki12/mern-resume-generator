
import React from 'react';
import { Builder } from "../../components";

const EditPage = () => {
  // basically the same as create page, but this gets the id from params
  return (
    <Builder />
  );
};

export default EditPage;